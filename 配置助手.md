# 📝 配置助手

## 当前状态
✅ Python 已安装
✅ 依赖已安装
❌ 配置未完成

---

## 下一步：填写配置

打开项目根目录的 `.env` 文件，填写以下配置：

### 必填项 1：钉钉配置（二选一）

#### 方式 A：自定义机器人（推荐新手）⭐

1. 打开钉钉，进入一个群聊
2. 点击右上角 `···` → `智能群助手` → `添加机器人` → `自定义`
3. 机器人名称填：`A股分析助手`
4. 安全设置选：`自定义关键词`，填写：`A股`
5. 复制生成的 Webhook 地址
6. 在 `.env` 文件中填写：

```env
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=你复制的token
```

#### 方式 B：企业应用（需要管理员权限）

如果你有企业管理员权限，可以使用这种方式：

1. 访问 https://open-dev.dingtalk.com/
2. 创建企业内部应用
3. 获取 AppKey、AppSecret、AgentId
4. 在 `.env` 文件中填写：

```env
DINGTALK_APPKEY=你的AppKey
DINGTALK_APPSECRET=你的AppSecret
DINGTALK_AGENT_ID=你的AgentId
```

---

### 必填项 2：AI 模型配置（选一个）

#### 选项 A：通义千问（推荐国内用户）⭐

1. 访问 https://dashscope.console.aliyun.com/
2. 开通服务并创建 API Key
3. 在 `.env` 文件中填写：

```env
OPENAI_API_KEY=sk-你的通义千问密钥
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MODEL_NAME=qwen-max
```

#### 选项 B：腾讯混元

1. 访问 https://console.cloud.tencent.com/hunyuan
2. 开通服务
3. 访问 https://console.cloud.tencent.com/cam/capi 创建密钥
4. 在 `.env` 文件中填写：

```env
OPENAI_API_KEY=你的SecretId:你的SecretKey
OPENAI_BASE_URL=https://api.hunyuan.cloud.tencent.com/v1
MODEL_NAME=hunyuan-lite
```

注意：混元的格式是 `SecretId:SecretKey`（用冒号连接）

#### 选项 C：DeepSeek（最便宜）

1. 访问 https://platform.deepseek.com/
2. 注册并获取 API Key
3. 在 `.env` 文件中填写：

```env
OPENAI_API_KEY=sk-你的DeepSeek密钥
OPENAI_BASE_URL=https://api.deepseek.com/v1
MODEL_NAME=deepseek-chat
```

#### 选项 D：OpenAI（需要国际网络）

```env
OPENAI_API_KEY=sk-你的OpenAI密钥
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_NAME=gpt-4
```

---

### 可选项：股票池和推送时间

这些已经有默认值，可以不改：

```env
# 关注的股票（默认已配置4只）
STOCK_POOL=000001,600519,000858,601318

# 推送时间（默认开盘后和收盘后）
PUSH_TIME=09:00,15:30
```

---

## 配置示例

### 完整示例（通义千问 + Webhook）

```env
# 钉钉配置
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=abc123xyz

# AI模型配置
OPENAI_API_KEY=sk-abc123xyz
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MODEL_NAME=qwen-max

# 股票配置（可选）
STOCK_POOL=000001,600519,000858,601318
PUSH_TIME=09:00,15:30
```

---

## 配置完成后

保存 `.env` 文件，然后运行：

```bash
python test_dingtalk.py
```

如果看到钉钉群收到测试消息，说明配置成功！

---

## 需要帮助？

- 钉钉配置详细步骤：`DINGTALK_SETUP.md`
- AI 模型配置详细步骤：`MODEL_CONFIG.md`
- 腾讯混元专用指南：`混元配置示例.md`
- 完整配置指南：`配置指南.md`
